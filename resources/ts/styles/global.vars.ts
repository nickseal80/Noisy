import colors from "./global.colors.vars";

export default {
    card: {
        background: colors.cardBackground,
        titleColor: colors.cardTitle,
        borderColor: colors.cardBorder,
        borderWidth: 2,
        borderRadius: 15,
        color: colors.cardText,
    },

    component: {
        background: colors.componentBackground,
        color: colors.componentText,
        labelColor: colors.cardText,
        fontSize: 11, //?
        padding: '15px 17px',
        borderRadius: 9,
        activeBackgroundColor: colors.componentActive,
    }
}
