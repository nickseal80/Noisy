import styled from "styled-components";
import colors from "../global.colors.vars";

export const StdBkg = styled.div`
    width: 100vw;
    height: 100vh;
    background-color: ${colors.background};
    color: ${colors.backgroundText};
`;
